<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css"
      integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="/css/home.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="title">TO-DO APP</h1>
      <!-- Form Section -->
      <form action="/add-task" method="post" class="form-section">
        <div class="form-row">
          <div class="form-group">
            <label for="description">DESCRIPTION</label>
            <input
              type="text"
              name="description"
              placeholder="What do you need to do?"
              required
            />
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label for="category">CATEGORY</label>
            <select name="category" required>
              <option>Choose a category</option>
              <option>Personal</option>
              <option>School</option>
              <option>Work</option>
              <option>Cleaning</option>
              <option>Others</option>
            </select>
          </div>
          <div class="form-group">
            <label for="dueDate">Due Date</label>
            <input type="date" name="dueDate"  required/>
          </div>
        </div>
        <div class="form-actions">
          <button class="btn add" type="submit">
            <i class="fas fa-plus"></i> Add Task
          </button>
        </form>
        <!-- <button class="btn delete" type="button">
          <i class="fas fa-trash"></i> Delete Tasks
        </button> -->
        <a href="/delete-task" id="delete-task">
            <button class="btn delete" type="button">
          <i class="fas fa-trash"></i> Delete Tasks
        </button>
        </a>
        </div>

      <hr />
      <!-- task list -->
      <ul class="task-list">
        <% for (task of tasks) { %> 
        
        
            <li>
              <input type="checkbox" action="/delete-task/?id=<%= task._id %>" class="checkbox" />
              <div class="task-info">
                <p class="task-desc"><%= task.description %></p>
                <p class="task-date">
                  <i class="fa fa-calendar" aria-hidden="true"></i> <%= task.dueDate %>
                </p>
              </div>
              <span class="badge <%= task.category%>"><%= task.category%></span>
            </li>
        <%}%>
      </ul>
    </div>
    <script src="/js/script.js" type="text/javascript"></script>
  </body>
</html>
